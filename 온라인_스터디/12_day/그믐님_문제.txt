1. 프로시저의 정의를 간략하게 서술하시오.

2. 다음 괄호 안에 들어갈 가장 적합한 데이터 분석 함수의 종류를 쓰시오.
함수	설명
(  ①  )	여러 행 또는 테이블 전체 행으로부터 하나의 결괏값을 반환하는 함수
(  ②  )	소그룹 간의 소계 및 중계 등의 중간 합계 분석 데이터를 산출하는 함수
(  ③  )	데이터베이스를 사용한 온라인 분석 처리 용도로 사용하기 위해서 표준 SQL에 추가된 기능

3. 트리거가 무엇인지 간략하게 서술하시오.

4. 관리자가 'USER1'이라는 사용자에 대해 '학생' 테이블인 STUDENT의 데이터를 수정할 수 있는 권한을 부여하고 그 권한을 필요시 다른 사용자에게 권한 부여하는 권한까지 추가하려고 한다. 다음 밑줄에 들어갈 가장 적합한 키워드를 쓰시오.
GRANT UPDATE ___①___  STUDENT ___②___ USER1
___③___ GRANT OPTION;

5. 다음은 프로시저에서 사용하는 간단한 케이스문이다. 밑줄에 들어갈 가장 적합한 키워드를 쓰시오.
CASE 변수
   ___①___  값1 ___②___
       ___③___ 명령어;
   ELSE
       SET 명령어;
END CASE;

6. 보기에 있는 요소들로 사용자 정의함수를 순서대로 나열하시오.
<보기>
시작/종료부, 반환부, 예외부, 제어부, 선언부, SQL

7. 다음은프로시저, 트리거, 사용자 정의함수의 선언부에 대한 설명이다. 괄호 (     ) 안에 들어갈 가장 적합한 키워드를 쓰시오.
구성	설명
(  ①  )	DBMS 내에 객체(트리거, 함수, 프로시저)를 생성
(  ②  )	기존 프로시저/트리거/사용자 정의함수 존재 시에 현재 컴파일하는 내용으로 덮어씀
(  ③  )	변수의 입력/출력을 구분하고, IN/OUT/INOUT 3가지로 구성

8. 다음이 설명하는 가장 적합한 용어를 쓰시오.
SQL Mapping 기반 오픈 소스 Access Framework로 DBMS에 질의하기 위한 SQL 쿼리를 별도의 XML 파일로 분리하고 MApping을 통해서 SQL을 실행한다.

9. [Customers] 테이블에서 <City> 속성의 두 번째 글자가 "a"인 모든 레코드들을 출력하는 SQL문을 작성하시오.

10. [Products] 테이블에서 <Price> 속성의 값이 1000인 레코드의 수를 구하는 SQL문을 작성하시오.

11. [Customers] 테이블에서 <CustomerID> 속성의 튜플의 수와<County> 속성을 <Country>로 그룹을 지어 검색하고, <CustomerID>의 튜플 수를 기준으로 내림차순으로 나타내시오.

12. "myDB" 라는 이름으로 새로운 데이터베이스를 생성하는 SQL문을 작성하시오.

13. 학생(STUDENT) 테이블에 전기과 학생이 50명, 전산과 학생이 100명, 전자과 학생이 50명 있다고 할 때, 다음 SQL문 ①, ②, ③의 실행 결과로 표시되는 튜플의 수를 쓰시오. (단, DEPT 필드는 학과를 의미한다)
① SELECT DEPT FROM STUDENT;
② SELECT DISTINCT DEPT FROM STUDENT;
③ SELECT COUNT(DISTINCT DEPT) FROM STUDENT WHERE DEPT='전산과';

14. Sun Microsystems에서 1997년 2월 개발한 DBMS 접속 기술로, Java 언어로 다양한 종류의 데이터베이스에 접속하고 SQL문을 수행할 떄 사용되는 표준 API가 무엇인지 쓰시오.

15. 데이터베이스에 테이블을 생성하고자 한다. 다음 <처리 조건>에 부합하도록 괄호(①, ②, ③)를 채워 SQL문을 완성하시오.
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
<처리조건>
1. 참가번호, 이름, 국가번호로 구성된 <참가자> 테이블을 정의하는 SQL문을 작성하시오. 단, 제약 조건은 다음과 같다.
  -참가번호는 기본키이다.
  -이름은 중복된 값을 가질 수 없다.
  -국가번호는 <국가목록> 테이블의 부여번호를 참조하는 외래키로 사용된다.
  -<국가목록> 테이블에서 부여번호가 변경되면 국가번호 값도 같은 값으로 변경한다.
  -각 속성의 데이터형은 저장될 데이터에 적합하게 지정한다.
2. SQL문의 문법은 ISO/IEC 9075 표준을 따른다.
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
<SQL문>
CREATE TABLE 참가자
	(참가번호 INT (   ①   ),
	 이름 CHAR(10) (   ②   ),
	 국가번호 INT,
	 FOREIGN KEY(국가번호)   (   ③   ) 국가목록(부여번호)
		ON UPDATE CASCAED) ;

16. 다음은 <사원> 테이블에 대해 소속지점별 사원들의 성과점수 평균이 30을 넘어가는 지점명과 성과점수 평균을 조회하는 SQL문이다. 괄호 (①, ②)에 적합한 명령을 넣어 SQL문을 완성하시오.
SELECT 소속지점, AVG(성과점수) FROM 사원 (   ①   ) 소속지점 (   ②   ) AVG(성과점수) > 30;

17. 데이터베이스와 관련한 다음 <처리조건>에 부합하도록 SQL문의 괄호 (①, ②)에 알맞은 명령어를 쓰시오.
<처리 조건>
  1. 테이블명은 사원으로 정의한다.
  2. 사원 테이블의 구조는 다음과 같다.
<사원>
속성명	데이터타입	비고
성명	VARCHAR(10)	PRIMARY KEY
부서	VARCHAR(20)
경력	INT
주소	VARCHAR(20)
기본급	INT
3. 성명이 '홍길동', '경력'이 10인 사원의 인사 정보를 입력하는 SQL문을 완성하시오.
INSERT (   ①   ) 사원(성명, 경력) (   ②   )('홍길동', 10);

18. 다음은 데이터베이스에서 학생 테이블의 이름을 신입생 테이블명으로 변경하는 DDL문을 작성하고자 한다. 괄호 안에 들어갈 알맞은 명령어를 쓰시오.
(   ①   ) TABLE 학생 TO 신입생;
또는
ALTER TABLE 학생 (   ①   ) TO 신입생;

19. 트랜잭션의 원자성을 보장하면서 데이터의 무결성을 유지해주는 DCL 명령에 대한 설명이다. 괄호 안에 들어갈 알맞은 명령문을 쓰시오.
(   ①   ) 명령문은 트랜잭션의 수행된 결과를 실제 물리적인 공간이 디스크에 영구적으로 저장하기 위해 사용하며, (   ②   ) 명령문은 트랜잭션의 실행 실패 또는 취소로 인한 수행된 결과를 원복하는데 사용한다.

20. 다음 보기의 SQL코드는 사원들이 속한 부서번호별로 사원들을 그룹화하고, 평균 급여가 2,500,000 이상인 부서에 대해서 부서번호와 평균 급여, 최대 급여를 출력하는 SQL문을 작성하고자 한다. 괄호 안에 들어갈 알맞은 명령어를 쓰시오.
SELECT DEPTNO, AVG(SALALY)
FROM EMPLOYEE
GROUP BY DEPTNO
(   ①   ) AVG(SALALY) >= 2500000;  
